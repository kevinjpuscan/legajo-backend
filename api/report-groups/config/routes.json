{
  "routes": [
    {
      "method": "GET",
      "path": "/report-groups",
      "handler": "report-groups.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/report-groups/count",
      "handler": "report-groups.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/report-groups/:id",
      "handler": "report-groups.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/report-groups",
      "handler": "report-groups.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/report-groups/:id",
      "handler": "report-groups.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/report-groups/:id",
      "handler": "report-groups.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
